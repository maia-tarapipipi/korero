import React from 'react'

export default function Profile() {
  // will contain: DB call for username, name, proficiency level, profile photo
  // ternary -> if already user title will show 'my profile', if not user 'create profile'
  // should be a way of viewing others profiles

  return <div></div>
}
